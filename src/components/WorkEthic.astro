<section>
    <h1>Work Ethic</h1>
    <article>
        <p class="number">01</p>
        <h2>First meeting</h2>
        <p class="description">Understanding your vision, wishes and branding.</p>
    </article>
</section>


<style>

    h1 {
        font-family: var(--font-inter-regular);
        font-size: var(--font-size-l);
        color: var(--secundary-bordeaux);
        text-transform: uppercase;
        font-weight: 400;
        text-align: center;
        margin-top: 1em;
        @media (min-width: 768px) {
            margin-top: 7em;
        }
    }

    article {
        display: grid;
        grid-template-columns: 1fr 1fr;
        grid-template-rows: 1fr 1fr;
        column-gap: 1em;
        margin-top: 2em;
        padding: 2em;
    }

    .number {
        font-family: var(--font-anastasia-italic);
        font-size: 6.5em;
        color: var(--secundary-bordeaux);
        grid-column: 1 / 2;
        grid-row: 1 / 3;
    }

    h2 {
        font-family: var(--font-anastasia-italic);
        font-size: var(--font-size-m);
        color: var(--secundary-bordeaux);
        text-align: right;
    }

    .description {
        grid-column: 2 / 3;
        text-align: right;
        font-family: var(--font-inter-regular);
        font-size: var(--font-size-base);
        color: var(--secundary-bordeaux);
    }
</style>