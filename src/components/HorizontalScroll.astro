<script>
    import gsap from "gsap";
    import ScrollTrigger from "gsap/ScrollTrigger";
    
    gsap.registerPlugin(ScrollTrigger);

    const wrapper = document.querySelector('.wrapper');
    let scrollWidth = wrapper.offsetWidth;
    let amountToScroll = scrollWidth - window.innerWidth;
    let mm = gsap.matchMedia();

    mm.add("(prefers-reduced-motion: no-preference)", () => {
        const tween =  gsap.to(wrapper, {
            x: -amountToScroll,
            duration: 3,
            ease: 'none',
            }
        );
        
        ScrollTrigger.create({
            trigger: wrapper,
            start: "top 20%",
            end: "+=" + amountToScroll,
            pin: true,
            animation: tween,
            scrub: 1,
        });
            
    });    
</script>

<div class="wrapper">
    <section class="scroll-section">
        <h2 class="fazael">Fazael.</h2>
        <h2>A</h2>
        <h2>digital</h2>
        <h2>agency</h2>
        <h2>based</h2>
        <h2>in</h2>
        <h2>Amsterdam.</h2>
    </section>
</div>


<style>
    .wrapper {
        display: flex;
        width: max-content;
    }

    .scroll-section {
        display: flex;
        gap: 8em;
        padding: 4em 2em;
    }

    .fazael {
        text-transform: uppercase;
    }

    h2 {
        font-size: 15em;
        font-family: var(--font-inter-bold);
        color: var(--primary-bordeaux);

    }
</style>
