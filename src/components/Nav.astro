<header>
    <a class="logo">
        <img src="/src/assets/logo-home.svg" alt="Logo of FAZAEL CO" width="150" height="75">
    </a>

    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/">Our Work</a></li>
            <li><a href="/">Work ethic</a></li>
            <li><a href="/">Contact us</a></li>
        </ul>
    </nav>

    <button class="menu">
        <img src="/src/assets/icon-bordeaux.svg" alt="" width="30" height="30"> menu
    </button>

</header>


<style>
    header {
        display: flex;
        justify-content: space-between;
        background-color: var(--primary-bone);
        position: sticky;
        top: 0;
    }

    .logo {
        &:hover {
            cursor: pointer;
        }
    }

    nav {
        position: fixed;
        left: 0;
        right: 0;
        top: 0;
        bottom: 0;
        translate: -100% 0%;
        @media (min-width: 375px) {
            display: flex;
            justify-content: center;
            padding: 1em 0;
        }
    }

    ul {
        list-style: none;
        @media (min-width: 375px) {
            display: flex;
            justify-items: center;
            gap: 1em;
            padding: 1em 3em 1em 3em;
            background-color: var(--neutral-white);
            width: 25em; 
        }  
    }

    li {
        flex-direction: row;
    }

    a {
        font-family: var(--font-coco-gothic-regular);
        text-decoration: none;
        text-transform: uppercase; 
        color: var(--primary-bordeaux);
    }

    .menu {
        all: unset;
        display: flex;
        flex-direction: column;
        align-items: center;
        font-family: var(--font-coco-gothic-regular);
        color: var(--primary-bordeaux);
        padding: 1em;
        &:hover {
            cursor: pointer;
        }
    }
</style>