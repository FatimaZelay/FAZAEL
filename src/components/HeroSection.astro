<section class="hero">
    <h1 id="hero-title">
        We create for
        <em>female</em>
        entrepreneurs. You
        <em>visualize</em>
        We design & build your <em>website</em>
    </h1>
</section>

<style>
    .hero {
        min-height: 90vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        @media (min-width: 768px) {
            padding-top: 10em;
        }
    }

    .hero-content {
        max-width: 20em;
    }

    .hero h1 {
        line-height: 1.5;
        font-weight: 400;
        font-family: var(--font-inter-regular);
        color: var(--primary-bordeaux);
        max-width: 20em;
    }

    .script {
        font-family: var(--font-anastasia-italic);
        font-size: 3em;
    }
</style>
