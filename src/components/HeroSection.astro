<section class="hero">
    <div class="hero-content">
      <h1>
        We build high performing
        <span class="script">Websites</span>
        <br />
        and make your vision come into reality
      </h1>
    </div>
  </section>
   <style>
    .hero {
      min-height: 120vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 80px var(--page-padding);
    }
     .hero-content {
      max-width: 900px;
    }
     .hero h1 {
      line-height: 1.5;
      font-weight: 400;
      font-family: var(--font-sans);
      color: var(--primary-bordeaux);
    }
     .script {
      font-family: var(--font-script);
      font-size: 90px;
    }
  </style>
 